# Copilot 使用说明

## 写作 / 编辑规则

- 如果用户为某一条目提供了摘要/总结/“简述”（例如周报条目简介），**必须原封不动使用用户的文字**。
  - 不要改写、润色、同义改写、扩写、缩写、翻译或“优化”。
  - 只允许调整周边结构/格式（例如标题层级、空行、列表符号），但用户提供的简述内容必须保持完全一致。
  - 如果确实需要改动措辞（如错别字、语气、长度），必须先征求用户同意。
